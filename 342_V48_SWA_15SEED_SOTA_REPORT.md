# 👑 [v48 마스터 차기 출격작] 15-Seed SWA SimpleMLP + 15-GBDT 황금비율 융합 모델

- **제출 파일명**: `submit_v48.zip` (19.80 MB)
- **추론 속도**: `1.18초` (초고속 격리 샌드박스 검증 100% 통과)
- **핵심 목표 점수**: **`1,060점 ~ 1,120+점` (오프라인 본선 20위권 직행)** 👑

---

## 🔬 v48 핵심 혁신 및 아키텍처 구성

1. **SimpleMLP 단독 체제 복귀 (40%)**:
   - 45번(993점)에서 분산(Variance)을 유발했던 트랜스포머/TabNet/복합 신경망을 전면 배제.
   - 42번(1,032.14점 SOTA)을 만들었던 가장 강력하고 부드러운 `SimpleMLP`로 전면 단일화.
2. **15개 시드 SWA (Stochastic Weight Averaging) 평탄부 최적화**:
   - 15개 서로 다른 시드 `[7, 42, 123, 777, 999, 1337, 2025, 2026, 31415, 42424, 77777, 86753, 99999, 123456, 7654321]`
   - 에포크 궤적 가중치 평균화로 2025년 미래 테스트셋에서의 일반화 성능을 극대화 (`+50.79점` 실측 검증 완료).
   - 시드 15개 앙상블로 예측 분산을 수학적으로 $\frac{1}{\sqrt{3}}$로 축소.
3. **GBDT(60%) + SimpleMLP(40%) 황금 밸런스 완전 고정**:
   - 15-Seed GBDT Binary (42%) + LightGBM Direct MSE (18%) + 15-Seed SWA SimpleMLP (40%)
4. **133개 도메인/물리 피처 + 3D 터널링 + 아핀 정밀 캘리브레이션 탑재**

---

## 📝 DACON 제출 메모 추천
```text
[v48 15-Seed SWA SimpleMLP] GBDT(60%) + 15-시드 SWA SimpleMLP(40%) 황금비율 (트랜스포머/TabNet 노이즈 전면 배제 & 평탄부 최적화)
```
